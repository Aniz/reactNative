export const addList = (text) => {
    type = 'addList'
    text = text
}

export const cleanList = () => {
    type = 'cleanList'
}

export const inputItem = (text) => {
    type = 'inputItem',
    text = text
}